//Criamos nossa interface
//A interface vai tipar os do banco de dados
//Ela vai trabalhar entre o array imagens e os dados vindo da API

export interface Fotos {
  id: number;
  img: string;
  titulo: string;
}
